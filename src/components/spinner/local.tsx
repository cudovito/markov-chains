// import React from "react";
// import { Spinner } from "reactstrap";

// interface IProps {
//   isLoading: boolean;
// }

// const LocalSpinner = ({ isLoading }: IProps) => {
//   return isLoading ? <Spinner color="primary" /> : null;
// };

// export default LocalSpinner;
